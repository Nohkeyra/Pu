name: Build APK

on:
  push:
    branches:
      - main
      - develop
  pull_request:
    branches:
      - main
      - develop
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 60

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Cache Gradle packages
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}
          restore-keys: |
            ${{ runner.os }}-gradle-

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Install dependencies
        run: |
          npm ci || npm install
          npm install -g @capacitor/cli

      - name: Build web assets
        env:
          VITE_API_URL_ANDROID: https://restoran-wawasan-bio.onrender.com
          VITE_FIREBASE_API_KEY: ${{ secrets.VITE_FIREBASE_API_KEY }}
          VITE_FIREBASE_AUTH_DOMAIN: ${{ secrets.VITE_FIREBASE_AUTH_DOMAIN }}
          VITE_FIREBASE_PROJECT_ID: ${{ secrets.VITE_FIREBASE_PROJECT_ID }}
          VITE_FIREBASE_STORAGE_BUCKET: ${{ secrets.VITE_FIREBASE_STORAGE_BUCKET }}
          VITE_FIREBASE_MESSAGING_SENDER_ID: ${{ secrets.VITE_FIREBASE_MESSAGING_SENDER_ID }}
          VITE_FIREBASE_APP_ID: ${{ secrets.VITE_FIREBASE_APP_ID }}
          VITE_FIREBASE_MEASUREMENT_ID: ${{ secrets.VITE_FIREBASE_MEASUREMENT_ID }}
        run: |
          npm run build || npm run build:prod
          if [ ! -d "dist" ]; then
            echo "❌ Build failed - dist folder not found"
            exit 1
          fi

      - name: Sync Capacitor
        run: |
          npx cap sync android || {
            echo "⚠️ Capacitor sync failed, attempting to add android platform..."
            npx cap add android
            npx cap sync android
          }

      - name: Verify google-services.json
        run: |
          echo "Checking google-services.json..."
          if [ -f "android/app/google-services.json" ]; then
            echo "✅ android/app/google-services.json exists."
            ls -la android/app/google-services.json
          elif [ -n "${{ secrets.GOOGLE_SERVICES_JSON_BASE64 }}" ]; then
            echo "⚠️ Restoring google-services.json from secret..."
            mkdir -p android/app
            echo "${{ secrets.GOOGLE_SERVICES_JSON_BASE64 }}" | base64 -d > android/app/google-services.json
            echo "✅ google-services.json restored"
          else
            echo "❌ ERROR: android/app/google-services.json not found!"
            exit 1
          fi

      - name: Set app version
        run: |
          echo "VERSION_CODE=$(date +'%Y%m%d%H%M')" >> $GITHUB_ENV
          echo "VERSION_NAME=$(date +'%Y.%m.%d-%H%M')" >> $GITHUB_ENV

      # CRITICAL: Setup keystore for signing
      - name: Setup signing keystore
        env:
          KEYSTORE_BASE64: ${{ secrets.ANDROID_KEYSTORE_BASE64 }}
          KEYSTORE_PASSWORD: ${{ secrets.KEYSTORE_PASSWORD }}
          KEY_ALIAS: ${{ secrets.KEY_ALIAS }}
          KEY_PASSWORD: ${{ secrets.KEY_PASSWORD }}
        run: |
          if [ -n "$KEYSTORE_BASE64" ]; then
            echo "✅ Decoding provided keystore..."
            mkdir -p android/app
            echo "$KEYSTORE_BASE64" | base64 -d > android/app/release-key.jks
            chmod 600 android/app/release-key.jks
            echo "✅ Keystore saved to android/app/release-key.jks"
            
            # Update app/build.gradle to use the keystore
            cat > android/app/keystore.properties << EOF
            storePassword=$KEYSTORE_PASSWORD
            keyPassword=$KEY_PASSWORD
            keyAlias=$KEY_ALIAS
            storeFile=release-key.jks
            EOF
            
            # Modify build.gradle to load keystore properties
            if ! grep -q "keystore.properties" android/app/build.gradle; then
              sed -i '/apply plugin: .com.android.application./a \
            def keystoreProperties = new Properties()\
            def keystorePropertiesFile = rootProject.file("app/keystore.properties")\
            if (keystorePropertiesFile.exists()) {\
                keystoreProperties.load(new FileInputStream(keystorePropertiesFile))\
            }' android/app/build.gradle
            fi
            
            echo "✅ Keystore configured for signing"
          else
            echo "❌ ERROR: ANDROID_KEYSTORE_BASE64 secret is missing!"
            echo "Please add this secret in GitHub Settings → Secrets"
            exit 1
          fi

      - name: Setup Gradle permissions
        run: |
          cd android
          chmod +x gradlew

      - name: Build debug APK
        working-directory: ./android
        run: |
          ./gradlew assembleDebug --stacktrace || {
            echo "❌ Debug build failed. Check logs above."
            exit 1
          }

      - name: Build release APK (signed)
        working-directory: ./android
        run: |
          ./gradlew assembleRelease --stacktrace || {
            echo "❌ Release build failed. Check logs above."
            exit 1
          }

      - name: Build Bundle (AAB for Play Store)
        working-directory: ./android
        run: |
          ./gradlew bundleRelease --stacktrace || {
            echo "⚠️ Bundle build failed (continuing)"
          }

      - name: Upload debug APK
        uses: actions/upload-artifact@v4
        with:
          name: app-debug
          path: android/app/build/outputs/apk/debug/*.apk
          retention-days: 30

      - name: Upload release APK
        uses: actions/upload-artifact@v4
        with:
          name: app-release-signed
          path: android/app/build/outputs/apk/release/*.apk
          retention-days: 30

      - name: Upload Release Bundle
        uses: actions/upload-artifact@v4
        with:
          name: app-release-bundle
          path: android/app/build/outputs/bundle/release/*.aab
          retention-days: 30

      - name: Comment PR with artifacts
        if: github.event_name == 'pull_request' && success()
        uses: actions/github-script@v7
        with:
          script: |
            const runId = context.runId;
            const repo = context.repo;
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: repo.owner,
              repo: repo.repo,
              body: `✅ **APK Build Successful!** (Build #${runId})
              
              📦 **Available Artifacts:**
              - **Debug APK:** \`app-debug.apk\`
              - **Signed Release APK:** \`app-release-signed.apk\`
              - **Bundle (AAB):** \`app-release.aab\`
              
              🔗 **Download:** Go to **Actions** → Select this workflow run → Scroll down to **Artifacts** section
              
              ⏱️ **Build Time:** ${{ github.run_started_at }}`
            })

      - name: Notify on failure
        if: failure()
        uses: actions/github-script@v7
        with:
          script: |
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: `❌ **Build Failed!** 
              
              Check the workflow logs for details: 
              https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}`
            })
