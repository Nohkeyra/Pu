/**
 * Menu data module
 * Consolidated single source of truth is maintained in `@/constants/menu`.
 * This file re-exports the canonical types and datasets consistently.
 */
export {
  DEFAULT_MENU_ITEMS,
  MENU_ITEMS,
  FEATURED_MENU_ITEMS,
  type MenuItem,
  type FeaturedMenuItem,
} from '@/constants/menu';

