/**
 * Generate a srcSet string for responsive images.
 * Returns empty string as variants are no longer used.
 */
export function generateSrcSet(): string {
  return '';
}

/**
 * Get the base fallback src for an image.
 */
export function getFallbackSrc(basePath: string): string {
  return basePath;
}
