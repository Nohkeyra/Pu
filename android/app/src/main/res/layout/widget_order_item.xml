<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/item_root"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:paddingStart="6dp"
    android:paddingEnd="6dp"
    android:paddingTop="14dp"
    android:paddingBottom="14dp"
    android:background="@android:color/transparent">

    <!-- Subtle Status Accent Line on the left -->
    <ImageView
        android:id="@+id/item_status_stripe"
        android:layout_width="3.5dp"
        android:layout_height="36dp"
        android:layout_gravity="center_vertical"
        android:background="#D4A853"
        android:layout_marginEnd="8dp" />

    <!-- Two-line layout for Details and Menu -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:gravity="center_vertical">

        <!-- Line 1: Date DD/MM | Location 🚩 | Meal Abbreviation (BF/LN/HT/DN) -->
        <TextView
            android:id="@+id/item_meal_type"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textColor="@color/widget_primary_text"
            android:textSize="13.5sp"
            android:textStyle="bold"
            android:singleLine="true"
            android:ellipsize="end"
            android:text="15/09 | Putrajaya 🚩 | LN" />

        <!-- Line 2: Menu Makanan -->
        <TextView
            android:id="@+id/item_meal_location"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textColor="@color/widget_secondary_text"
            android:textSize="12.5sp"
            android:singleLine="true"
            android:ellipsize="end"
            android:text="Nasi Beriani, Ayam Masak Merah"
            android:layout_marginTop="2dp" />

    </LinearLayout>

    <!-- Hidden dummy elements to satisfy other Java bindings safely without crashes -->
    <TextView
        android:id="@+id/item_pax_badge"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:visibility="gone" />
    <TextView
        android:id="@+id/item_date"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:visibility="gone" />
    <TextView
        android:id="@+id/item_time"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:visibility="gone" />

</LinearLayout>
