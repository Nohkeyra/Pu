# Optional bearer token for direct MCP clients/testing.
# If set, requests to /mcp must include: Authorization: Bearer <token>
# Claude Custom Connectors normally use OAuth rather than a static bearer token.
MCP_ACCESS_TOKEN=
